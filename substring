package string;
/**
 * String substring(int start,int end)
 * 截取当前字符串中指定范围类的字符串
 * start与end时两个下标。
 * 表示从那里截取到 哪里
 * 注意：java API有一个特点，
 * 通常两个数字表示范围时
 * 都是“含头不含尾”的。即：
 * 包含开始位置对应内容，但不包含
 * 结束位置对应内容。
 * @author soft01
 *
 */
public class Substring {

	public static void main(String[] args) {
		//								0123456789
		String host = "ww.tedu.cn";
		
		String name = host.substring(4, 8);
		System.out.println(name);
		
		name = host.substring(4);
		System.out.println(name);
	}

}
